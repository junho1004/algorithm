#include <iostream>
using namespace std;

int N;
int map[21][21];

void input() {
	cin >> N;
	for (int y = 0; y < N; y++) {
		for (int x = 0; x < N; x++) {
			cin >> map[y][x];
		}
	}
}

int main() {
	freopen_s(new FILE*, "input.txt", "r", stdin);



	return 0;
}